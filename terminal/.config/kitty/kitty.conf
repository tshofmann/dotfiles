# ============================================================
# kitty.conf - Kitty Terminal Konfiguration
# ============================================================
# Zweck       : GPU-beschleunigtes Terminal mit Image-Support
# Pfad        : ~/.config/kitty/kitty.conf
# Docs        : https://sw.kovidgoyal.net/kitty/conf/
# Reload      : Ctrl+Cmd+, oder kill -SIGUSR1 $KITTY_PID
# Config-UI   : Cmd+, (öffnet diese Datei im Editor)
# ============================================================

# ------------------------------------------------------------
# Theme: Catppuccin Mocha (eingebaut seit v0.26)
# ------------------------------------------------------------
# Kitty hat alle Catppuccin-Flavors eingebaut!
# Kein externes Theme-File nötig.
# Wechsel interaktiv: kitten themes
# Wechsel direkt:     kitten themes --reload-in=all Catppuccin-Mocha
include current-theme.conf

# ------------------------------------------------------------
# Font: MesloLGS Nerd Font (konsistent mit Starship/eza)
# ------------------------------------------------------------
# Font-Auswahl interaktiv: kitten choose-fonts

font_family      MesloLGS Nerd Font Mono
bold_font        auto
italic_font      auto
bold_italic_font auto
font_size        13.0

# Ligatures aktivieren (für Fira Code etc.)
disable_ligatures never

# ------------------------------------------------------------
# macOS Spezifika
# ------------------------------------------------------------

# Option als Alt-Taste (für ZSH Word-Movement: Alt+← Alt+→)
macos_option_as_alt yes

# Titlebar passend zum Theme (Catppuccin Mocha Base)
macos_titlebar_color #1E1E2E

# Traditioneller Fullscreen (schneller als macOS-Animation)
macos_traditional_fullscreen yes

# Quit wenn letztes Fenster geschlossen (macOS-Verhalten)
macos_quit_when_last_window_closed yes

# Fenster-Titel: Tab-Title in Titlebar und Menubar
macos_show_window_title_in all

# ------------------------------------------------------------
# Cursor
# ------------------------------------------------------------

cursor_shape beam
cursor_blink_interval 0.5
cursor_stop_blinking_after 15.0

# ------------------------------------------------------------
# Scrollback
# ------------------------------------------------------------

scrollback_lines 10000

# Scrollback im Pager öffnen: Ctrl+Shift+H
scrollback_pager less --chop-long-lines --RAW-CONTROL-CHARS +INPUT_LINE_NUMBER

# ------------------------------------------------------------
# Window
# ------------------------------------------------------------

# Padding für bessere Lesbarkeit
window_padding_width 8

# Fenstergröße merken
remember_window_size yes

# Bestätigung beim Schließen (nur wenn Prozesse laufen)
# -1 = Shell-Prompts werden nicht gezählt (braucht Shell-Integration)
confirm_os_window_close -1

# ------------------------------------------------------------
# Bell
# ------------------------------------------------------------

# Kein Audio-Bell (nur visuell über Tab-Badge)
enable_audio_bell no
visual_bell_duration 0

# Tab zeigt 🔔 wenn Bell in unfokussiertem Fenster
bell_on_tab "🔔 "

# ------------------------------------------------------------
# Shell Integration (automatisch für ZSH)
# ------------------------------------------------------------
# Features:
#   - Jump to Prompt: Ctrl+Shift+Z/X
#   - Last Command Output: Ctrl+Shift+G
#   - Click to move cursor in prompt
#   - CWD in Tab-Titel
#   - Beam-Cursor am Prompt

shell_integration enabled

# ------------------------------------------------------------
# Tab Bar
# ------------------------------------------------------------

tab_bar_edge bottom
tab_bar_style powerline
tab_powerline_style slanted
tab_bar_min_tabs 2

# Tab-Titel: Index + Titel (für goto_tab Shortcuts)
tab_title_template "{index}: {title}"

# ------------------------------------------------------------
# URLs
# ------------------------------------------------------------

# URLs unterstreichen beim Hover
detect_urls yes
underline_hyperlinks hover

# URL-Style (curly = gewellt wie in IDEs)
url_style curly

# ------------------------------------------------------------
# Performance
# ------------------------------------------------------------

# VSync für flüssiges Scrolling
sync_to_monitor yes

# GPU-Rendering (Standard auf macOS: Metal)
# Keine explizite Einstellung nötig

# ------------------------------------------------------------
# Keyboard Shortcuts
# ------------------------------------------------------------
# Kitty-Modifier: Ctrl+Shift (Standard)
# macOS nutzt zusätzlich Cmd-Kombinationen

# Die meisten Shortcuts sind sinnvoll voreingestellt.
# Wichtige Defaults:
#   Cmd+T           Neuer Tab
#   Cmd+W           Tab schließen
#   Cmd+Enter       Neues Fenster (Split)
#   Cmd+1-9         Tab 1-9 wechseln
#   Cmd+,           Config editieren
#   Ctrl+Shift+H    Scrollback im Pager
#   Ctrl+Shift+G    Letzten Command-Output im Pager
#   Ctrl+Shift+Z    Zum vorherigen Prompt springen
#   Ctrl+Shift+X    Zum nächsten Prompt springen
#   Ctrl+Shift+E    URL öffnen (Hints)

# Reload config (zusätzlich zu Ctrl+Shift+F5)
map cmd+shift+r load_config_file
